-- HACK: We should be able to pass args into the GetScenarioConfig rather than duplicate the whole campaign.
function GetProductInfo()
    return {
        Game = "HoN",
        Name = "gazette_0",
    }
end

#include <objects>
#include <guids>
#include <Scenarios/Campaign>
